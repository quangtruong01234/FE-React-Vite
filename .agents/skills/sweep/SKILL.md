---
name: sweep
description: Weekly backlog sweep — fix the top open item(s) from snapshot.md and the backend handoff inbox end-to-end, audit-only for new issues, or propose features. Use for recurring maintenance passes.
---

Read `.ai/workflows/sweep.md` and all referenced context completely, then execute the sweep. Treat an optional argument (a number, an item id like `P2-05`/`F4`, `audit [area]`, or `propose`) as the mode selector per the workflow.

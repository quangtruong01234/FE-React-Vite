---
name: verify-ui
description: Verify a running page visually with browser tooling and report layout or computed-style issues. Use when asked to verify rendered UI behavior.
---

Read `.ai/workflows/verify-ui.md`, `.ai/context/styling.md`, and `.ai-local/test-accounts.md` completely, then execute the protocol using browser tools available in the current session. Treat URL, focus, and login inputs as command arguments. Report only; never auto-fix.
